<div dir="rtl">

# تمرين الأسبوع ٢

## القسم ١ — Linear Algebra (NumPy)

اكتب كود يحل:

١. أنشئ vector `v = [3, 4]`. احسب الطول (`||v||`).
٢. أنشئ matrix `A` من البعد `(3, 3)` بقيم عشوائية. احسب الـ inverse + الـ transpose.
٣. أنشئ vectorين، احسب الـ cosine similarity بينهم بدون استخدام scikit-learn.
٤. اضرب matrix `(2, 3)` في matrix `(3, 4)`. وش بعد النتيجة؟ ليش؟

## القسم ٢ — Probability

اكتب simulation بـ NumPy يجاوب:

١. لو رميت عملة ١٠٠ مرة، كم توقعك تحصل "صورة"؟ كرر التجربة ١٠٠٠ مرة وارسم histogram.
٢. ولد ١٠٠٠ عينة من توزيع طبيعي بمتوسط ٥٠ وانحراف ١٠. احسب نسبة العينات بين ٤٠ و٦٠.
٣. مسألة Bayes: لو ٢٪ من الناس عندهم مرض معين، والاختبار دقته ٩٥٪، وشخص طلع نتيجته إيجابية، كم احتمال إنه فعلاً مريض؟

## القسم ٣ — Statistics

استخدم dataset الـ Titanic من الأسبوع ١:

١. اعمل t-test بين أعمار اللي نجوا واللي ماتوا. هل الفرق ذو دلالة إحصائية؟
٢. احسب correlation بين `Fare` و `Survived`. ايش معنى الرقم؟
٣. أعطِ مثال على correlation ≠ causation من نفس الـ dataset.

## التسليم

Notebook في repo `ds-week-02-<اسمك>` + LinkedIn post اختياري.

</div>
